# Redis Schema

challenge:session:{sessionId} challenge:vqa:history:{sessionId}
telemetry:trajectory:{sessionId}:{challengeId} risk:score:{sessionId}
cache:api:{key}

## TTL 정책

challenge session: 20 min\
trajectory: 10 min\
risk score: session lifetime
